/** @type {import('next').NextConfig} */
const nextConfig = {
  // Nothing unusual here — the SEC proxy runs in the same Next.js process,
  // so no additional rewrites or external image domains needed.
};

export default nextConfig;
